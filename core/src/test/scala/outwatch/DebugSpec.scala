package outwatch

object DebugSpec extends JSDomSuite{

//  test("Perf") {
//    (0 to 10) foreach { _ =>
//      val elemId = "msg"
//
//      val handler = BehaviorSubject[Int](0)
//
////        Handler.create[Int](0).unsafeRunSync
////      val handler2 = Handler.create[Int](0).unsafeRunSync
////      val handler3 = Handler.create[Int].unsafeRunSync
//
//      val vtree = div(
////        id := elemId,
////        span(id := "pete", "Go!"),
//        //        onClick foreach {},
//        //        onDomMount foreach {},
//        //        onDomUnmount foreach {},
//        //      dsl.cls <-- handler.map(_.toString),
//        //      dsl.value <-- handler.map(_.toString),
//        handler.map { i =>
//          (0 to i).map { j =>
//            div("Test")
//          }
//          //        input(tpe := "text", dsl.defaultValue := i.toString, styleAttr := "background:black;")
//        }
////        div(
////        handler2.map { i =>
////          (0 to i).map { j =>
////            div(
////              div("hans", cls := j.toString, handler3),
////              p(p()),
////              handler3
////            )
////          }
////        }
////        )
//      )
//
//      val node = document.createElement("div")
//      document.body.appendChild(node)
//
//      val t = System.nanoTime()
//
//      OutWatch.renderInto(node, vtree).unsafeRunSync()
//
//
//            println(node.innerHTML)
//
//      val t2 = System.nanoTime()
//      handler.onNext(10000)
//      val t3 = System.nanoTime()
////      println(node.innerHTML.length)
////      println(node.innerHTML.take(1000))
//
//      //      handler2.unsafeOnNext(1001)
////      val t4 = System.nanoTime()
//
////      println("TOOK =====> " + (t2 - t).toDouble/1000000)
//      println("SINGLE1 TOOK =====> " + (t3 - t2).toDouble/1000000)
////      println("SINGLE2 TOOK =====> " + (t4 - t3).toDouble/1000000)
//    }
//  }


}
